@model ThisNetWorks.OrchardCore.Seo.TwitterMeta.ViewModels.TwitterMetaPartViewModel
@{
    if (!String.IsNullOrEmpty(Model.Title))
    {
        <meta asp-name="title" content="@Model.Title" />
    }
    @*if (!String.IsNullOrEmpty(Model.MetaKeywords))
    {
        <meta asp-name="keywords" content="@Model.MetaDescription" />
    }*@
}
